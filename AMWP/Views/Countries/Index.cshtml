@model IEnumerable<AMWP.Models.Countries>

@{
    ViewBag.Title = "Index";
}

<h2 class="alert alert-info text-center">Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CountryID }) |
                @Html.ActionLink("Details", "Details", new { id = item.CountryID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.CountryID })
            </td>
        </tr>
    }

</table>


@section CSS{
    <style>
        .alert-info {
            color: #0c5460;
            background-color: #efeea4;
            border-color: #bee5eb;
        }
    </style>
}
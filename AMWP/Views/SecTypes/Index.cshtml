@model IEnumerable<AMWP.Models.SecTypes>

@{
    ViewBag.Title = "Index";
}

<h2 class="alert alert-info text-center">Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.TypeID }) |
                @Html.ActionLink("Details", "Details", new { id = item.TypeID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.TypeID })
            </td>
        </tr>
    }

</table>


@section CSS{
    <style>
        .alert-info {
            color: #0c5460;
            background-color: #efeea4;
            border-color: #bee5eb;
        }
    </style>
}